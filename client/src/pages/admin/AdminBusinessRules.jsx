import ComingSoon from '../../components/dashboard/ComingSoon.jsx';

export default function AdminBusinessRules() {
  return (
    <ComingSoon
      title="Business Rules"
      description="Configuration for promotion boosts and ranking constraints (per the SRS business rules, e.g. bounded promotion multipliers) will appear here in a later section."
    />
  );
}