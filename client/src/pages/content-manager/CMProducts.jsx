import ComingSoon from '../../components/dashboard/ComingSoon.jsx';

export default function CMProducts() {
  return (
    <ComingSoon
      title="Product Management"
      description="A manageable table of Cartify's product catalogue (edit, deactivate, view) will appear here in a later chunk, built on the existing /api/products endpoints."
    />
  );
}