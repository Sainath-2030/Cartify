import ComingSoon from '../../components/dashboard/ComingSoon.jsx';

export default function CMProductNew() {
  return (
    <ComingSoon
      title="Add Product"
      description="A form for uploading a new item — name, description, category, price, specifications and images — will appear here in a later chunk."
    />
  );
}