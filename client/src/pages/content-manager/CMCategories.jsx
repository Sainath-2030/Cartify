import ComingSoon from '../../components/dashboard/ComingSoon.jsx';

export default function CMCategories() {
  return (
    <ComingSoon
      title="Category Management"
      description="Tools to create, edit and reorder Cartify's categories will appear here in a later chunk, built on the existing /api/categories endpoints."
    />
  );
}